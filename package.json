{
  "name": "ore-format-repo",
  "private": true,
  "description": "ORE (Open Real Estate) \u2014 open file format and reference engine for CRE underwriting data. Repo-level tooling; publishable packages live in their own directories.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p engine",
    "validate": "node validator/cli.mjs validate examples",
    "cli": "node validator/cli.mjs",
    "golden": "node engine/golden/run-golden.mjs",
    "golden:update": "node engine/golden/run-golden.mjs --update",
    "llms": "node spec/build-llms-txt.mjs",
    "test": "npm run build && npm run validate && npm run golden",
    "demo": "npx -y http-server -c-1 -o /demo/ ."
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "typescript": "^6.0.3"
  }
}
